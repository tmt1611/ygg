// This file consisted only of TypeScript type definitions and interfaces.
// It is intentionally empty after transpilation to JavaScript.
export {}; // Ensures it's treated as a module if imported, though types are gone.
